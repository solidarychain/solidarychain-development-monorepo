query participantById($id: String!) {
  participantById(id: $id) {
    id
    name
    msp
    participant {
      id
      name
      msp
    }
    identities {
      id
      status
      fingerprint
    }
    createdDate    
  }
}
